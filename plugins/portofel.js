
//Requiring Packages
const Discord = require('discord.js'); //This can also be discord.js-commando or other node based packages!
// const core = require('../economy.js'); //This can also be discord.js-commando or other node based packages!

// const client = require('../run.js')(currency);
module.exports = {
	name: 'portofel',
	// description: 'Ia-ti spaga',
async	execute(message, args){
  // const target = message.mentions.users.first() || message.author;
  // return message.channel.send(`${message.author} has ${currency.getBalance(message.author)}💰`)
  console.log('portofel');
}
};
